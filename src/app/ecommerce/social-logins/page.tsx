import SociaLogins from '@/features/pages/settings/social-logins'

export default async function SocialLoginPage() {
    // let taxes = []
    // try {
    //     const res = await getTaxes()
    //     taxes = res ? res.data : []
    // } catch (error) {
    //     console.error('Fetch error:', error);
    // }
    return (
        <SociaLogins />
    )

}