import SliderList from '@/features/pages/management/appearance/slider'

export default async function SliderListPage() {
    // let taxes = []
    // try {
    //     const res = await getTaxes()
    //     taxes = res ? res.data : []
    // } catch (error) {
    //     console.error('Fetch error:', error);
    // }
    return (
        <SliderList />
    )

}