import ShippingMethods from '@/features/pages/settings/shipping-methods'

export default async function ShippingMethodsPage() {
    // let taxes = []
    // try {
    //     const res = await getTaxes()
    //     taxes = res ? res.data : []
    // } catch (error) {
    //     console.error('Fetch error:', error);
    // }
    return (
        <ShippingMethods />
    )

}