import StorefontList from '@/features/pages/management/appearance/storefont'

export default async function SliderListPage() {
    // let taxes = []
    // try {
    //     const res = await getTaxes()
    //     taxes = res ? res.data : []
    // } catch (error) {
    //     console.error('Fetch error:', error);
    // }
    return (
        <StorefontList />
    )

}